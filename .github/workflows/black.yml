name: Black

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
        
    - name: Python Blacken
      uses: piotrpawlaczek/python-blacken@release/stable
      with:
        path: '.'
        line-length: 81
